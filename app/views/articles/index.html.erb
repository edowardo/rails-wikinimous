<% @articles.each do |article| %>
  <div class="articleListing">
    <h2 class="articleTitle"><%= link_to article.title, article_path(article)  %></h2>
    <p><%= article.content.first(250) %></p>
  </div>
<% end %>
